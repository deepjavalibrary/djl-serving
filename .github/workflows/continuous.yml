name: Continuous

on:
  pull_request:
    paths-ignore:
      - "**.md"
      - "**.ipynb"
      - "**.json"
      - "**.html"
      - "**.js"
      - "**.css"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Echo Python Path
        run: echo $PATH
      - name: Which python
        run: which python
      - name: run python server
        run: /usr/bin/python /home/runner/work/djl-serving/djl-serving/serving/src/main/python/python_server.py --sock-type "tcp" --host "127.0.0.1" --port "9000"
      - uses: actions/checkout@v2
      - name: Set up JDK 1.8
        uses: actions/setup-java@v1
        with:
          java-version: 1.8
      # Enable gradle cache: https://github.com/actions/cache/blob/master/examples.md#java---gradle
      - uses: actions/cache@v2
        with:
          path: ~/.gradle/caches
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*') }}
          restore-keys: |
            ${{ runner.os }}-gradle-
      - name: Build with Gradle
        run: ./gradlew build jRR jRV
      - name: Upload test results
        uses: actions/upload-artifact@v1
        if: always()
        with:
          name: serving
          path: serving/build/reports
